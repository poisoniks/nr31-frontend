---
trigger: always_on
---

# Code Completeness
- Write clean, production-ready, and fully functional code.
- Prohibited: Never leave placeholders, mock logic, comments like "add your logic here", or incomplete `// TODO` blocks. 
- Implementation: Always implement the full underlying logic, anticipating edge cases such as token refresh failures, API errors (handled via red toast notifications), and empty states.